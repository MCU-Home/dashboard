# frontend/

The MCUHome Dashboard frontend: a TypeScript single-page application.

The framework and build tooling are selected during the design phase — see
[ADR 0002](../docs/adr/0002-python-backend-typescript-frontend.md). This
directory is a placeholder until that decision is made.
